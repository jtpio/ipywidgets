name: Test

on: [push, pull_request]

jobs:
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: |
            **/setup.cfg
            **/requirements*.txt
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'
      - name: Install dependencies
        run: |
          sudo apt-get install -y pandoc
          python -m pip install --upgrade pip
          python -m pip install -e ./python/ipywidgets
          python -m pip install -r ./docs/requirements.txt
      - name: Build docs
        run: |
          cd docs/source
          python -m sphinx -T -E -b html -d ../build/doctrees -D language=en . ../build/html
  js:
    name: JavaScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: |
            **/setup.cfg
            **/requirements*.txt
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install jupyterlab==4.0.0 jupyter_builder
      - name: yarn install, build, test
        run: |
          jlpm --immutable
          jlpm build
          jlpm build:examples

          pushd packages/base
          jlpm test:unit:firefox:headless
          popd

          pushd packages/base-manager
          jlpm test:unit:firefox:headless
          popd

          pushd packages/controls
          jlpm test:unit:firefox:headless
          popd

          pushd packages/html-manager
          jlpm test:unit:firefox:headless
          popd

          pushd examples/web1
          jlpm test:firefox:headless
          popd

        env:
          CI: true
  python:
    name: Python
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.x']

    steps:
      - uses: actions/checkout@v7
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: |
            **/setup.cfg
            **/requirements*.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ./python/ipywidgets[test]
      - name: Test with pytest
        run: |
          cd python/ipywidgets
          pytest --cov=ipywidgets ipywidgets

  spec:
    name: Message Specification
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: |
            **/setup.cfg
            **/requirements*.txt
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install jupyterlab -e ./python/ipywidgets
      - name: Install JS dependencies
        run: |
          jlpm
      - name: Compare spec with latest version
        run: |
          python ./packages/schema/generate-spec.py -f markdown spec.md
          jlpm prettier
          diff -u ./packages/schema/jupyterwidgetmodels.latest.md ./spec.md

  labextension-compat:
    name: Labextension smoke test on JupyterLab ${{ matrix.jupyterlab || 'latest' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Oldest and newest supported JupyterLab 4.x
        jupyterlab: ['==4.0.*', '']

    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: |
            **/setup.cfg
            **/requirements*.txt
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "jupyterlab${{ matrix.jupyterlab }}" jupyter_builder

      - name: Build and install the extension
        run: |
          jlpm --immutable
          jlpm build
          pip install ./python/jupyterlab_widgets
          jupyter labextension list

      - name: Install test dependencies
        working-directory: ui-tests
        run: |
          jlpm --immutable
          npx playwright install chromium

      - name: Run labextension smoke test
        working-directory: ui-tests
        run: |
          npx playwright test tests/plugin.test.ts

  ui-test:
    name: Visual Regression
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: |
            **/setup.cfg
            **/requirements*.txt
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -U jupyterlab==4.6.2 jupyter_builder

      - name: Build and Install ipywidgets
        run: |
          jlpm --immutable
          jlpm build

      - name: Build the extension
        run: |
          cd python/ipywidgets
          pip install .
          cd ../..
          jlpm
          jlpm build
          cd python/jupyterlab_widgets
          pwd
          pip install -e .
          jupyter labextension develop . --overwrite
          jupyter labextension list

      - name: Install Test Dependencies
        run: |
          jlpm --immutable
          npx playwright install chromium
        working-directory: ui-tests

      - name: Execute integration tests
        shell: bash -l {0}
        working-directory: ui-tests
        run: |
          npx playwright test

      - name: Upload Playwright Test assets
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ipywidgets-test-assets
          path: |
            ui-tests/test-results

      - name: Upload Playwright Test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ipywidgets-test-report
          path: |
            ui-tests/playwright-report

      - name: Update snapshots
        if: failure()
        working-directory: ui-tests
        run: |
          jlpm test:update

      - name: Upload updated snapshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ipywidgets-updated-snapshots
          path: ui-tests/tests
